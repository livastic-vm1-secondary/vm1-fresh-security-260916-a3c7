merge_queue:
  max_parallel_checks: 1

queue_rules:
  - name: vm1-method-freshness
    merge_method: merge
    batch_size: 1
    queue_conditions:
      - base = vm1-method-base-260924
      - title = VM1_MERGE_METHOD_CONFIG_FRESHNESS_260924
    merge_conditions:
      - check-success = vm1-method-hold

pull_request_rules:
  - name: VM1 fresh baseline
    conditions:
      - -draft
    actions: {}
